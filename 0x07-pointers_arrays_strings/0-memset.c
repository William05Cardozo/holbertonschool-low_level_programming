#include "main.h"
#include <stdio.h>
/**
 * _memset - Entry Point
 * @s: Pointer
 * @b: Variable
 * @n: Variable
 * a: Varaible
 * Return: No line
 */

char *_memset(char *s, char b, unsigned int n)
{
	int c;

	for (c = 0; c < n; a++)
	{
		s[c] = b;
	}
	return (s);
}
